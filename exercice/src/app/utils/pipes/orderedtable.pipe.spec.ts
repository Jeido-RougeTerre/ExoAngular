import { OrderedtablePipe } from './orderedtable.pipe';

describe('OrderedtablePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderedtablePipe();
    expect(pipe).toBeTruthy();
  });
});
